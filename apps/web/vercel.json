{
  "buildCommand": "cd ../.. && NODE_ENV=development pnpm install --ignore-scripts && pnpm --filter @bibliology/web build",
  "outputDirectory": "dist",
  "installCommand": "NODE_ENV=development pnpm install --ignore-scripts",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
